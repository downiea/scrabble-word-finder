# Vision fixture files

Raw Claude API response JSON files, captured by running CaptureFixtureTest.
These are committed to git so VisionExtractionTest can run offline without API calls.

Naming convention: `{gameConfigId}_sample.json`
